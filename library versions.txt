os: Já faz parte da biblioteca padrão do Python e não está na sua lista, então a versão varia com a versão do Python que você está usando.

pandas: 2.0.3 (import pandas as pd)

numpy: 1.23.5 (import numpy as np)

tensorflow: 2.12.0 (import tensorflow as tf)

Bio: 1.81 (from Bio.Align import PairwiseAligner e from Bio.SeqUtils.ProtParam import ProteinAnalysis)

tensorflow.keras.preprocessing.sequence: 1.1.2 (from tensorflow.keras.preprocessing.sequence import pad_sequences)

sklearn.metrics: 1.0.2 (from sklearn.metrics import confusion_matrix, precision_score, recall_score, f1_score, roc_auc_score, accuracy_score)

tensorflow.keras.models: 2.12.0 (from tensorflow.keras.models import load_model)

matplotlib.pyplot: 3.5.1 (import matplotlib.pyplot as plt)

datetime: Já faz parte da biblioteca padrão do Python e não está na sua lista, então a versão varia com a versão do Python que você está usando.
